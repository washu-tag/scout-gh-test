apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: storage-loki-0 # NAME MUST MATCH WHAT LOKI IS LOOKING FOR UNFOURTUNATELY
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 25Gi
  storageClassName: local-path
  